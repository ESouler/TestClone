#!/bin/bash
#changes
    num=0
    # создаю переменную, которая будет выполнять команду 
    # которая выводит список директории
    # `` внутри этих знаков выполняется вписанная команда
    list_of_files=$(ls $*) # or `ls` 
    
    printf "These are the files in your directory:\n" # выводит в новой строке
    # вот эту надпись 

    for files in $list_of_files: # так мы создаем for loops
    # которая будет выводит список директории и еще добавлять к каждой новой строке число строки 
     do # начинаем действие которое мы хотим в for loop
      echo "$num: ${files1}" # 
      num=$((++num)) # берет переменную num и к ней прибавляет еденицу 
    # тем самым к каждой строке будет прибавлятся еденица 
     done 
